{
    "name": "${modName}",
    "entry": "${modFile}",
    "version": "${modVersion}",
    "type": "native",
    "optionalDependencies": [
        {
            "name": "more-dimensions"
        }
    ]
}